/// Development-only helpers. Never imported from [main] in Phase 02.
/// Must not run in production release builds.
library;

/// Intentionally empty. Prototype seed (Highlands, Grab, salary, …) is not used here.
const bool kEnableDevTransactionSeed = false;
