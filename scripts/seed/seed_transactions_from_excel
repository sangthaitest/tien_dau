#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT/seed"
dart pub get --offline >/dev/null 2>&1 || dart pub get
exec dart run seed_transactions_from_excel.dart "$@"
